# Environment Configuration
# APP_ENV options: local, development, staging, production
#
# local (DDEV/local development):
#   - Always uses local assets (no CDN)
#   - Enables Whoops error handler
#   - Best for DDEV and local development
#
# development/staging (dev/staging servers):
#   - Uses CDN if CDN_ENABLED=true
#   - Can enable Whoops for debugging
#   - Good for testing CDN integration
#
# production (live site):
#   - Uses CDN if CDN_ENABLED=true
#   - Production error handling
#   - Recommended: CDN_ENABLED=true
APP_ENV=local

# CDN Configuration
# Set CDN_ENABLED=true to use BunnyCDN (or other CDN) for static assets
# Note: When APP_ENV=local, CDN is always disabled regardless of this setting
CDN_ENABLED=false
CDN_BASE_URL=https://sunnysideac.b-cdn.net

# Vite Development Server Configuration (for DDEV)
VITE_DEV_SERVER_PROTOCOL=http
VITE_DEV_SERVER_HOST=localhost
VITE_DEV_SERVER_PORT=3000
